<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= post.title %></title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; max-width: 800px; margin: auto; }
    h1 { color: #333; }
    .meta { color: #777; font-size: 0.9rem; margin-bottom: 10px; }
    .tags span {
      background-color: #eee;
      padding: 4px 8px;
      margin-right: 5px;
      border-radius: 4px;
      font-size: 0.85rem;
    }
    .back-link {
      display: inline-block;
      margin-top: 20px;
      color: blue;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <h1><%= post.title %></h1>

  <div class="meta">
    <p><strong>By:</strong> <%= post.author.first_name %> <%= post.author.last_name %></p>
    <p><strong>Reading time:</strong> <%= post.reading_time %> min</p>
    <p><strong>Read count:</strong> <%= post.read_count %></p>
  </div>

  <p><strong>Description:</strong> <%= post.description %></p>

  <hr>

  <div>
    <p><%= post.body %></p>
  </div>

  <hr>

  <div class="tags">
    <strong>Tags:</strong>
    <% post.tags.forEach(tag => { %>
      <span><%= tag %></span>
    <% }) %>
  </div>

  <a class="back-link" href="/userposts">← Back to Posts</a>
</body>
</html>
